/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
                'assets.pokemon.com',
                'lh3.googleusercontent.com'],
    },
}

module.exports = nextConfig
